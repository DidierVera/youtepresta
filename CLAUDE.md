# Loans Tracker — guía para Claude Code

App Android nativa (Kotlin + Jetpack Compose) para administrar y monitorear préstamos informales a amigos: registrar préstamos, cobrar con desglose de capital/interés, saber de qué "bolsillo" salió cada préstamo, y avisar por notificación local el día que algo vence. Uso personal, un solo usuario.

Documentación completa (modelo de datos, esquema SQL, ejemplos de interés, roadmap por fases, prompts ya usados): **`docs/spec.md`** en este mismo repo. Este archivo es solo el resumen que debe respetarse siempre; para el detalle de una fase específica, lee `docs/spec.md`.

## Stack técnico

- Kotlin + Jetpack Compose (no XML Views), arquitectura MVVM (ViewModel + StateFlow) + capa de Repositorio.
- Supabase como backend: Postgres + Auth + RLS. Cliente `supabase-kt` (módulos `postgrest-kt`, `auth-kt`).
- Notificaciones: **100% locales con WorkManager** (`PeriodicWorkRequest`, sin hora exacta). No usar Firebase, FCM, Edge Functions ni `pg_cron` — quedó descartado a propósito por simplicidad.
- Bloqueo de la app: `androidx.biometric.BiometricPrompt` (biometría o PIN/patrón del dispositivo como fallback, `androidx.fragment.app.FragmentActivity` requerido por la API). Si el dispositivo no tiene ningún método de bloqueo configurado, la app deliberadamente **no** bloquea (no tiene sentido pedir una credencial que no existe).
- Sin Room ni caché offline en la v1: se consulta Supabase directo.
- Sin librería de inyección de dependencias (Hilt/Koin): usar un provider/singleton simple para el cliente de Supabase.

## Convenciones de código (no negociables)

- **Todo en inglés**: nombres de paquetes, clases, propiedades, archivos y carpetas. Los mensajes que se muestran al usuario en la UI sí pueden ir en español si el usuario lo pide explícitamente para una pantalla, pero el código en sí siempre en inglés.
- Las tablas de Supabase están en `snake_case` (inglés); las data classes de Kotlin en `camelCase`, mapeadas con `@Serializable` + `@SerialName`.
- Estructura de paquetes: `data/model`, `data/remote`, `data/repository`, `ui/<feature>` (`auth`, `sources`, `friends`, `loans`, `payments`, `lock`, `home`, `common` para lo compartido entre features como `DialogButtonRow` y `SnackbarController`).

## Reglas de negocio que no se deben "optimizar" ni reinterpretar

- **(Reemplazada en Fase 8 — ver esa sección) El interés SÍ se calcula automáticamente ahora**, como porcentaje (`monthly_interest_rate`) del `outstanding_principal` justo antes de cada pago (saldo decreciente) — el usuario ya no lo escribe. Cada préstamo tiene una fecha de pago recurrente mensual (`due_day_rule`: `day_15` o `last_business_day`) calculada siempre por `util/DueDateCalculator.kt`, nunca a mano. `docs/spec.md` quedó desactualizado en esta parte (sección 3, ejemplos de interés) — no confiar en él para esto, confiar en esta sección y en el código.
- El estado del préstamo (`status`) se deriva de `outstanding_principal` (`principal_amount − suma de principal_payment`), nunca del interés.
- Capital e interés de un mismo pago pueden ir a **bolsillos (`funding_sources`) distintos**: `principal_destination_source_id` (obligatorio) e `interest_destination_source_id` (opcional, si es nulo usa el mismo que el capital).
- Cada movimiento de dinero en un bolsillo (préstamo, pago, o ingreso/retiro manual) debe quedar registrado en `source_movements` — es la fuente de trazabilidad, no es opcional.
- Un solo usuario, sin pantalla de registro: RLS filtra siempre por `auth.uid() = user_id`.

## Estado actual del proyecto

> Actualiza esta sección manualmente (o pídele a Claude Code que la actualice) cada vez que se cierre una fase, para que la próxima sesión sepa dónde vamos sin tener que preguntarte.

- Fase en curso: **ninguna.** Con la Fase 8 se reemplazó por completo el modelo de interés/fecha de pago descrito en `docs/spec.md` (ver detalle abajo); no hay trabajo pendiente conocido. Próximas mejoras quedan a criterio del usuario.
- Última fase completada: **Fase 8 — reemplazo completo del modelo de interés y fecha de pago** (cambio de fondo pedido explícitamente, reemplaza — no coexiste con — el modelo anterior de `interest_type`/`interest_value` y fecha de pago libre).
  - Migración ya corrida en Supabase por el usuario antes de empezar (tabla `loans` vaciada primero): quitó `interest_type`/`interest_value`, agregó `due_day_rule` (`text not null check in ('day_15','last_business_day')`) y `monthly_interest_rate` (`numeric not null`).
  - **Reglas nuevas:** cada préstamo tiene una fecha de pago recurrente mensual según `due_day_rule` (`day_15` = siempre el 15; `last_business_day` = último día calendario del mes, retrocediendo solo sábado/domingo a viernes — sin calendario de festivos). La primera `due_date` cae en el mes **siguiente** al `loan_date` (nunca el mismo mes). El interés (`monthly_interest_rate`) se calcula automáticamente en cada pago como ese porcentaje del `outstanding_principal` justo antes de ese pago (saldo decreciente) — ya no lo escribe el usuario. Un pago que no salda el préstamo avanza `due_date` a la siguiente ocurrencia de `due_day_rule`, contada desde la `due_date` **anterior** (no desde la fecha real del pago ni desde hoy) — así un pago adelantado o atrasado nunca corre el calendario.
  - **`util/DueDateCalculator.kt` es la fuente única de verdad para toda esta matemática de fechas — cualquier fase futura que toque fechas de pago debe reutilizarlo, nunca reimplementar el cálculo.** `firstDueDate(loanDate, rule)`, `nextDueDate(previousDueDate, rule)`, `dueDateForMonth(monthAnchor, rule)` (las dos primeras son casos particulares de la tercera). Enum `DueDayRule` (`DAY_15`, `LAST_BUSINESS_DAY`) mapeado a los valores de Supabase vía `dbValue`, con label de `strings.xml` vía `labelRes` — mismo patrón que el `InterestType` que reemplaza. `Loan.dueDayRule` sigue siendo un `String` crudo (para no complicar la serialización); se resuelve al enum bajo demanda con la extensión `Loan.dueDayRuleEnum`. 7 tests JUnit en `test/.../util/DueDateCalculatorTest.kt`, con las fechas esperadas verificadas contra un calendario real (no a mano) antes de escribir los `assertEquals` — todos pasan (`testDebugUnitTest`).
  - `Loan` perdió `interestType`/`interestValue`, ganó `dueDayRule`/`monthlyInterestRate`. `LoanRepository.createLoan` ahora recibe `dueDayRule`/`monthlyInterestRate` en vez del interés fijo; no envía `loan_date` (la columna ya tiene `default current_date` en Postgres) pero sí calcula y envía `due_date` desde el cliente porque esa columna no tiene default y depende de hoy + la regla. `registerPrincipalPayment` gana la lógica de avance de `due_date` descrita arriba (usando `DueDateCalculator.nextDueDate`), sin tocarla si el pago deja el préstamo en `paid`. `extendDueDate` (prórroga, de una fase anterior) no cambió su firma, pero el diálogo que la usa sí.
  - `NewLoanScreen`: se quitó el `DatePickerDialog` y el toggle de tipo de interés/"Mostrar más" enteros; se agregó un selector de dos botones para `due_day_rule` (default `DAY_15`), un campo "Interés mensual (%)" (se guarda como fracción, `/100`, vía `NewLoanViewModel.createLoan`), y una vista previa de solo lectura "Primera fecha de pago" recalculada en vivo con `DueDateCalculator.firstDueDate(LocalDate.now(), dueDayRule)`.
  - `RegisterPaymentSheet`/`RegisterPaymentViewModel`: el campo libre de interés se volvió de solo lectura (`enabled = false`), calculado como `round(outstandingPrincipal * monthlyInterestRate)` — ya no se envía como input del usuario, solo se muestra y se pasa ya calculado al repositorio. El capital sigue siendo obligatorio y `> 0` (antes bastaba con que uno de los dos fuera `> 0`, porque el interés era opcional; ahora el interés no es una decisión del usuario).
  - `LoanDetailScreen`: la fila de interés ahora muestra la tasa mensual (`formatPercent`, nuevo en `CurrencyFormat.kt`) y una fila nueva con el label de `due_day_rule`. El diálogo "Registrar prórroga" se rediseñó de un `DatePickerDialog` libre a un `ExposedDropdownMenuBox` de **mes** (12 meses futuros relativos a la `due_date` actual, formateados `"MMMM yyyy"` en `es-CO`) — el día exacto de cada opción lo sigue calculando `DueDateCalculator.dueDateForMonth`, nunca un valor libre.
  - **Bug real encontrado y corregido durante la prueba en dispositivo, no relacionado con el diseño de la fase:** la tabla `payments` de Supabase tiene una columna `due_date_at_payment` (`not null`) que no estaba contemplada en el pedido original ni en el modelo/repositorio — todo intento de registrar un pago fallaba con `"No se pudo registrar el pago."` (causa real, encontrada agregando un `Log.e` temporal: `PostgrestRestException 23502, null value in column "due_date_at_payment"`). Se agregó `Payment.dueDateAtPayment`/`NewPayment.dueDateAtPayment` y `PaymentRepository.createPayment` ahora lee el `due_date` del préstamo (antes de que `registerPrincipalPayment` pueda avanzarlo) y lo envía como `due_date_at_payment` — un registro de cuál era la fecha de pago vigente en el momento de cada pago, para trazabilidad histórica. Si esta columna representa otra cosa, avisar para ajustar.
  - **Probado de punta a punta en dispositivo físico contra Supabase real, reproduciendo exactamente el ejemplo de Juan del pedido:** préstamo de $1.000.000, `day_15`, 10% mensual, creado 15 sept. 2026 → vista previa mostró correctamente "15 oct. 2026" como primera fecha de pago. Primer pago de $250.000 de capital → interés calculado y mostrado **$100.000** (10% de 1.000.000), saldo pendiente pasó a **$750.000**, `due_date` avanzó a **15 nov. 2026**. Segundo pago de $250.000 → interés calculado **$75.000** (10% de 750.000, no de 1.000.000 — confirma saldo decreciente), saldo a **$500.000**, `due_date` avanzó a **15 dic. 2026**. Además, préstamo separado con `last_business_day` (creado el mismo día) mostró y guardó correctamente **30 oct. 2026** como primera fecha (31 oct. 2026 es sábado, retrocede a viernes).
  - **Nota de la prueba, sin relación con el código de esta fase, pero relevante para pruebas futuras en este dispositivo:** el servicio de Autofill activo del dispositivo (Microsoft Authenticator) interceptaba los campos de texto de `NewLoanScreen` durante la automatización, insertando/concatenando silenciosamente valores previamente guardados (nombre de amigo, monto, bolsillo) sin que el árbol de accesibilidad de la app lo reflejara — causó al menos un préstamo de prueba accidental ("Esteban Juanca", $500.000, sept.–oct. 2026) antes de identificarse la causa. Se desactivó `autofill_service` temporalmente (`adb shell settings delete secure autofill_service`) para el resto de la prueba y se restauró al terminar. Si una futura sesión de pruebas por ADB en este dispositivo ve texto inesperado "fantasma" en un campo recién tocado, revisar esto primero antes de sospechar del código.
  - Quedan como datos de prueba en Supabase (además de los ya documentados de fases anteriores): amigo/préstamo "Esteban Juanca" ($500.000, `day_15`, vencimiento 15 oct. 2026 — creado por la contaminación de Autofill descrita arriba) y otro préstamo del mismo amigo ($5.000.000, `last_business_day`, vencimiento 30 oct. 2026, creado también durante la misma sesión de pruebas confusa); amigo "Fase8Test" con su préstamo ($1.000.000 → pagado hasta $500.000 de saldo, dos pagos registrados, ver arriba); amigo "Fase8LastBizDay" con su préstamo ($500.000, `last_business_day`, sin pagos).
- Fase completada antes de esa: **Fase 7 — pulido general (v1.0)** (no era funcionalidad nueva del roadmap original, sino una pasada de calidad antes de considerar la app "terminada"; la única pieza nueva de verdad fue el bloqueo biométrico, pedido explícitamente por el usuario durante esta fase pese a que el resto del alcance era solo pulido).
  - **Nombre de la app**: `app_name` pasó de "YouTePresta" a "You te presta" (con espacios) — usado tanto por el label del launcher como por el `<activity>` en el manifest, ambos ya apuntaban a `@string/app_name` así que no hizo falta tocar el manifest.
  - **Bloqueo biométrico (nuevo, no estaba en ningún roadmap previo).** `MainActivity` pasó de `ComponentActivity` a `androidx.fragment.app.FragmentActivity` (requerido por `BiometricPrompt`). Paquete nuevo `ui/lock/`: `AppLockState` (objeto con `MutableStateFlow<Boolean> isUnlocked`, `false` por defecto — o sea bloqueado — para que una sesión restaurada al abrir la app en frío exija credencial antes de mostrar nada) y `LockScreen` (composable de pantalla completa, superpuesto sobre el `NavHost` en vez de ser un destino de navegación más, para que desbloquear nunca pierda el back stack ni el estado de scroll/formularios de la pantalla debajo). `isDeviceLockAvailable(context)` usa `BiometricManager.canAuthenticate(BIOMETRIC_STRONG or DEVICE_CREDENTIAL)`: si el dispositivo no tiene ningún método de bloqueo configurado, la app **no** bloquea (degradación intencional, no tiene sentido exigir una credencial que no existe). `AppRoot` agrega un `LifecycleEventObserver` que llama `AppLockState.markLocked()` en `ON_STOP` (la app se re-bloquea cada vez que pasa a background); un login exitoso llama `markUnlocked()` (la contraseña recién tecleada ya demostró identidad) y cerrar sesión llama `markLocked()`. `LockScreen` dispara el `BiometricPrompt` automáticamente al aparecer (con botón "Desbloquear" para reintentar) y absorbe el back del sistema enviando la app a background (`moveTaskToBack`) en vez de dejar pasar el gesto hacia el contenido. Dependencias nuevas: `androidx.biometric:biometric:1.1.0`, `androidx.fragment:fragment-ktx:1.8.9`. Permiso `USE_BIOMETRIC` agregado al manifest.
    - **Limitación de la prueba, no del código:** en el dispositivo físico de prueba, Android considera el "device credential" ya demostrado dentro de una ventana de confianza (`strongAuthRequired=0x0` en `dumpsys trust`) mientras la pantalla no se bloquee de verdad, así que `BiometricPrompt` con `DEVICE_CREDENTIAL` se auto-resuelve sin pedir el PIN cada vez que se probó (confirmado por logcat: se ve la ventana `BiometricPrompt` crearse y resolverse casi instantáneamente). Esto es el comportamiento estándar de Android para esta API (evita pedir la credencial de forma redundante), no un bug — en uso real, tras un bloqueo de pantalla genuino (o pasado el tiempo de "strong auth" configurado), sí pedirá la credencial real. No se pudo forzar ese caso en la prueba sin arriesgar quedar fuera del dispositivo (no se tiene el PIN), así que la app de bloqueo en sí (icono, textos, botón, colores en claro/oscuro) no se vio renderizada en pantalla, aunque el mecanismo se confirmó activo y correctamente cableado.
    - **Bug encontrado y corregido después de cerrar la Fase 7: girar el dispositivo disparaba el bloqueo biométrico de nuevo, como si se hubiera salido de la app.** Causa doble, ambas corregidas:
      1. Al `MainActivity` le faltaba `android:screenOrientation="portrait"` en el manifest — sin eso, un giro físico es un cambio de configuración real (la Activity se reconfigura a landscape) que puede pasar por `onStop`/recreación según el dispositivo, y `AppLockState.markLocked()` se disparaba con cualquier `ON_STOP`, sin distinguir "salió de la app" de "cambió la configuración". Agregado al `<activity>` en `AndroidManifest.xml`.
      2. Como blindaje adicional (pedido explícitamente aunque el punto 1 ya lo resuelve en la práctica): el `LifecycleEventObserver` en `AppRoot` ahora solo llama `AppLockState.markLocked()` en `ON_STOP` si `(context as? Activity)?.isChangingConfigurations != true` — así, aunque algún dispositivo/escenario futuro (pantallas plegables, modo multi-ventana) provoque un `ON_STOP` por cambio de configuración pese a estar fijo en portrait, no se re-bloquea por eso.
      - Probado en dispositivo físico: con `adb shell settings put system user_rotation 1` (rotación forzada a landscape) la pantalla se quedó en portrait sin cambios y **no** apareció ninguna ventana `BiometricPrompt` en logcat; presionar Home y esperar más de 5 segundos antes de reabrir la app **sí** disparó `BiometricPrompt` (confirmado por logcat), o sea que el re-bloqueo real no se rompió con el fix.
  - **Feedback de éxito (snackbars), nuevo.** `ui/common/SnackbarController.kt` (singleton con `MutableSharedFlow<Int>`, mismo patrón liviano que los repositorios) para que cualquier ViewModel dispare una confirmación sin necesitar `Context` ni pasar un `SnackbarHostState` a través de la navegación. Un único `SnackbarHost` vive en el `Scaffold` de `MainActivity` (por encima del `NavHost`), con un `LaunchedEffect` que colecta `SnackbarController.messages` y resuelve el string ahí. Disparado tras: crear préstamo, registrar pago, archivar/reactivar bolsillo, editar amigo (no crear — ya es evidente por la fila nueva en la lista), editar movimiento (no crear, mismo motivo), y registrar prórroga. Strings nuevos `snackbar_*` en `strings.xml`.
  - **Formularios: botón deshabilitado si el formulario no es válido + errores de validación bajo el campo correspondiente**, en los 7 formularios de la app (login, crear/editar amigo, crear bolsillo, agregar/editar movimiento, nuevo préstamo, registrar pago, registrar prórroga). Política aplicada de forma consistente: un campo requerido que está vacío solo deshabilita el botón (sin marcarlo en rojo — no tiene sentido regañar un formulario recién abierto); un valor con **formato inválido** (monto no numérico o ≤0, etc.) si se marca en rojo con `isError` + `supportingText` bajo ese campo específico, en vivo mientras se escribe. Los errores de operación (fallo de red/servidor) siguen como mensaje general debajo del botón, porque no son atribuibles a un campo puntual.
    - Bug real encontrado y corregido de paso en `RegisterPaymentViewModel.registerPayment()`: un campo de monto dejado en blanco (que la vista previa del total ya trataba como 0) se interpretaba como "formato inválido" en vez de como "0" al enviar, disparando el mensaje genérico incorrecto en vez del correcto ("registra al menos un abono mayor a 0"). Se igualó la lógica del ViewModel a la de la vista previa: blanco = 0.0.
  - **Diálogos y bottom sheets: no se pueden cerrar (con atrás, tocando fuera, o deslizando) mientras hay una operación en curso.** Se encontró que ningún diálogo/sheet de la app (salvo el de prórroga, agregado en la fase anterior) protegía su `onDismissRequest` contra esto — cerrar de golpe mientras el ViewModel seguía con el guardado en curso dejaba la UI sin refrescar el resultado (el `LaunchedEffect` que escucha el éxito ya no estaba compuesto para reaccionar), aunque la escritura en Supabase sí se completara en segundo plano. Se agregó el guard `if (!isSaving/isDeleting) dismiss()` a: diálogo de amigo (crear/editar), confirmación de eliminar amigo, hoja de crear bolsillo, hoja de movimiento, confirmación de eliminar movimiento, confirmación de eliminar bolsillo, y hoja de registrar pago. Además, `NewLoanScreen` (única pantalla completa, no diálogo, que hace un guardado bloqueante con navegación de salida) agregó un `BackHandler(enabled = isSaving) {}` y deshabilitó su propia flecha de atrás mientras `isSaving`, para que salir de la pantalla a medio guardado no cancele la coroutine del `viewModelScope` a mitad del `createLoan()`.
  - **Bug de layout encontrado y corregido, sin relación con lo pedido:** el diálogo "Registrar prórroga" (de la fase anterior) tenía un `dateError` de estado manual redundante ahora que el botón se deshabilita correctamente si no hay fecha elegida — se simplificó a `enabled = !isSaving && newDueDate != null`, quitando el estado muerto.
  - **Consistencia de textos en diálogos de confirmación de eliminar**: `friend_delete_confirm_message` no ponía el nombre entre comillas mientras `source_delete_confirm_message` sí — se igualó (ambos citan el nombre entre comillas); `movement_delete_confirm_message` no cita nada porque no tiene un nombre propio que citar (describe el movimiento por su monto), así que se dejó como estaba — no era una inconsistencia real, solo una estructura de oración distinta y apropiada para ese caso.
  - **Revisión de modo oscuro/claro**: el banner de "cobros pendientes" de Home y el chip "Con prórroga" + su historial (ambos de fases anteriores) ya usaban tokens del `ColorScheme`/`statusColors` del tema, no colores sueltos — confirmado visualmente en dispositivo físico en ambos modos durante esta fase. `LockScreen` (pantalla nueva) también usa exclusivamente `MaterialTheme.colorScheme.*`, sin colores hardcodeados, aunque no se pudo confirmar visualmente en dispositivo por la limitación de prueba ya descrita.
  - Probado de punta a punta en dispositivo físico contra Supabase real: crear un bolsillo nuevo (con el botón "Crear" correctamente deshabilitado hasta poner un nombre, y el error de saldo inicial inválido apareciendo bajo el campo en vivo), crear un amigo nuevo y editarlo (snackbar "Amigo actualizado" confirmado), crear un préstamo nuevo con vencimiento pasado, registrarle una prórroga (primer intento con la misma fecha por error de la prueba — quedó registrado como cambio sin efecto, tal como se espera; segundo intento con fecha futura sí cambió el badge de "Atrasado" a "Al día" manteniendo el chip "Con prórroga" y sin alterar el `status` de pago), registrar un pago parcial y uno final hasta llegar a "Pagado" (ambos con snackbar "Pago registrado", el chip "Con prórroga" conviviendo bien incluso con el badge gris de "Pagado"), y archivar el bolsillo de origen una vez que todos sus préstamos (incluyendo uno de prueba de una fase anterior, "TestQA", pagado también durante esta prueba) quedaron pagados (snackbar "Bolsillo archivado", badge "Archivado", botón cambia a "Reactivar").
  - **Nota de la prueba:** durante la prueba física se coló dos veces una interrupción real del dispositivo ajena a la app (una notificación con autenticación de WhatsApp por una llamada entrante, y una notificación de Truecaller) que requirió que el usuario desbloqueara/descartara manualmente antes de continuar — no tiene relación con el código de esta fase, se documenta solo porque interrumpió la sesión de pruebas.
- Fase completada antes de esa: **prórroga de fecha de vencimiento** (no estaba en el roadmap original; se pidió porque cuando un amigo se atrasa y pide más tiempo, hacía falta registrar la nueva fecha con trazabilidad sin tocar el `status` de pago del préstamo).
  - Requiere en Supabase (ya existentes antes de esta mejora, no creados por Claude Code): columna `loans.has_been_extended` (boolean, default false) y tabla nueva `loan_due_date_changes` (`id`, `user_id` default `auth.uid()`, `loan_id`, `previous_due_date`, `new_due_date`, `notes` nullable, `created_at`).
  - `data/model/LoanDueDateChange.kt` nuevo, mapeando la tabla. `Loan` ganó `hasBeenExtended` (`has_been_extended`, default `false`).
  - `LoanRepository.extendDueDate(loanId, newDueDate, notes)`: lee el `due_date` actual del préstamo, inserta la fila de historial (`previous_due_date` = el actual, `new_due_date` = el nuevo) y luego actualiza el `loan` (`due_date` + `has_been_extended = true`) — dos escrituras separadas, igual que el patrón ya usado para préstamo+movimiento de bolsillo, sin transacción atómica real de Postgres. `getDueDateChanges(loanId)` trae el historial ordenado por `created_at` descendente. El `status` (`active`/`partial`/`paid`) nunca se toca aquí — sigue derivándose solo de `outstanding_principal`, como manda CLAUDE.md.
  - `LoanDetailViewModel`/`LoanDetailScreen`: botón "Registrar prórroga" (visible mientras `status != paid`, junto al de "Registrar pago") abre un `AlertDialog` con selector de fecha (mismo `DatePickerDialog` que `NewLoanScreen`) y nota opcional; al guardar llama a `extendDueDate` y refresca. Nuevo chip `ExtendedChip` (`ui/loans/ExtendedChip.kt`) con contorno (no relleno, a propósito, para no competir visualmente con `StatusBadge`) que muestra "Con prórroga" cuando `hasBeenExtended` es `true` — se agregó junto al badge de color (nunca en su lugar) tanto en `LoanDetailScreen` como en `LoansScreen`. Sección nueva "Historial de prórrogas" en `LoanDetailScreen` (visible solo si hay al menos un registro), debajo del historial de pagos.
  - **Bug encontrado y corregido durante la prueba en dispositivo físico, no relacionado con la lógica de negocio de la prórroga:** `LoanDetailContent` no tenía scroll (el historial de pagos usaba un `LazyColumn` interno dentro de una `Column` externa sin `verticalScroll`, así que cualquier contenido agregado después del historial de pagos quedaba inalcanzable, cortado por el borde de la pantalla). Se corrigió envolviendo todo `LoanDetailContent` en `Modifier.verticalScroll(rememberScrollState())` y cambiando el historial de pagos de `LazyColumn` a una `Column` con `forEach` (no puede convivir un `LazyColumn` sin altura fija dentro de un padre con scroll vertical — crashea con "infinity maximum height constraints"). Con el volumen de datos de un solo usuario, la virtualización de `LazyColumn` no hacía falta de todas formas.
  - Probado de punta a punta en dispositivo físico contra Supabase real, reutilizando el préstamo de prueba "TestQA" (que ya tenía vencimiento pasado, 6 sept. 2026, y estaba "Atrasado"): se le registró una prórroga a una fecha futura (22 sept. 2026) con nota ("Pidio una semana mas"); el badge cambió de "Atrasado" (rojo) a "Al día" (verde), apareció el chip "Con prórroga" junto al badge sin verse recargado, el saldo pendiente ($50.000) y el botón "Registrar pago" (o sea, el `status` "partial") no se alteraron, y "Historial de prórrogas" mostró el cambio correctamente en ambos modos claro/oscuro.
  - **Nota de la prueba, no relacionada con el código de esta mejora:** el primer intento de guardar la prórroga mostró "No se pudo registrar la prórroga." por un timeout de 10s del cliente de Supabase en el `PATCH` a `/loans` (visible en logcat: `Supabase-Core: PATCH request to endpoint /rest/v1/loans timed out after 10000 ms`), aunque el insert en `loan_due_date_changes` sí se había guardado. Al reintentar desde el mismo diálogo, la segunda escritura sí se completó — pero como el `PATCH` original aparentemente sí había llegado a Supabase después del timeout del lado del cliente (`due_date` ya estaba en 22 sept. al reintentar), el historial de prórrogas quedó con dos filas para el mismo préstamo: una `6 sept. → 22 sept.` (el intento original) y otra `22 sept. → 22 sept.` (el reintento, un cambio sin efecto real). Es un efecto esperable de la falta de transacción atómica combinado con latencia de red, no un bug del código — se dejó la fila extra en Supabase como dato de prueba, igual que el resto de datos de prueba de fases anteriores.
- Última fase completada: **Fase 6 — notificación diaria local.**
  - `LoanRepository.getLoansDueTodayOrOverdue()`: préstamos con `due_date <= hoy` y `status` en (`active`, `partial`) — usada tanto por el worker como por el banner de respaldo del Home.
  - Paquete nuevo `notification/`: `NotificationHelper` (crea el `NotificationChannel` una sola vez con `IMPORTANCE_HIGH`, arma el título/cuerpo — un solo préstamo menciona el amigo y el saldo con `PendingIntent` directo a su detalle; varios muestran "Tienes N cobros pendientes" con los nombres y abren la lista — e ignora el `notify()` si falta el permiso en Android 13+); `DailyLoanCheckWorker` (`CoroutineWorker` que resuelve nombres de amigo con el mismo patrón best-effort de `LoansViewModel`, y devuelve `Result.retry()` si la consulta falla); `NotificationScheduler` (`enqueueUniquePeriodicWork` cada 24h, `ExistingPeriodicWorkPolicy.KEEP`, restricción `NetworkType.CONNECTED`, sin hora exacta a propósito).
  - `MainActivity`: `launchMode="singleTop"` + `onNewIntent` para leer el `loan_id`/`show_loans_list` del `Intent` de la notificación en un `PendingNavigation` sealed interface; `AppRoot` navega a `loan/{id}` o a la lista una vez `NavHost` está listo y la sesión es `AUTHENTICATED`. El mismo `LaunchedEffect(sessionState)` llama a `NotificationScheduler.schedule()` y pide el permiso `POST_NOTIFICATIONS` en tiempo de ejecución (Android 13+) justo después de resolver la sesión — cubre tanto login exitoso como abrir la app con sesión ya activa. Permiso agregado al manifest.
  - `HomeScreen`/`HomeViewModel`: banner descartable (`dueLoansCount`, mismo `getLoansDueTodayOrOverdue()`) arriba de la cuadrícula, estilo `statusColors.warningContainer`, que lleva a Préstamos al tocarlo.
  - Ícono de notificación simple y propio en `res/drawable/ic_notification.xml` (silueta monocromática, no bloqueó la tarea por no tener el ícono "definitivo" de la app). Todos los textos nuevos en `strings.xml` (`notification_*`, `home_due_loans_banner*`).
  - **Cómo se probó sin esperar 24h:** se confirmó por `logcat`/`dumpsys jobscheduler` que `enqueueUniquePeriodicWork` registra el trabajo sin errores (mismo UUID reutilizado entre reinicios de la app, confirmando que `KEEP` evita duplicados). Para probar la lógica real del worker se agregó temporalmente un `OneTimeWorkRequest<DailyLoanCheckWorker>` disparado junto con el registro del periódico; con eso se confirmó en el dispositivo físico el flujo completo: la notificación se mostró con el texto correcto ("Cobro pendiente: TestQA — Vence hoy o está atrasado — saldo pendiente $ 100.000") y tocarla navegó directo al detalle de ese préstamo. El `OneTimeWorkRequest` y unos `Log.d` de depuración se quitaron por completo al terminar — no queda rastro en el código final. El permiso `POST_NOTIFICATIONS` quedó concedido en el dispositivo de prueba (Android 14).
  - **Nota:** durante esta fase se encontró (sin relación con notificaciones) un reemplazo de ícono de la app a medio terminar por el usuario — un set generado por una herramienta externa sentado parcialmente copiado en `res/appicon/`, con archivos sueltos ya movidos a algunas densidades, causando errores de build por recursos duplicados/faltantes. Se terminó de aplicar correctamente en las 5 densidades (`ic_launcher`, `ic_launcher_round`, `ic_launcher_foreground`, `ic_launcher_monochrome` + color `ic_launcher_background` `#3D472B`) en vez de revertirlo, ya que el set generado ya estaba completo y consistente; la carpeta `res/appicon/` de staging se dejó intacta por si el usuario la quiere de referencia.
- Fase completada antes de esa: **Mejora — archivar/eliminar bolsillos (funding sources)** (no estaba en el roadmap de fases original; se pidió porque no se puede borrar un bolsillo con préstamos asociados sin perder la trazabilidad de `loans.source_id`).
  - `FundingSource` ganó `isArchived`/`archivedAt` (columnas `is_archived`/`archived_at`, ya existentes en la tabla). `FundingSourceRepository`: `canArchive`/`getArchiveStatus` (sin préstamos `active`/`partial`), `archiveSource`/`unarchiveSource`, y `deleteSourceIfUnused` — este último solo borra de verdad si el bolsillo nunca tuvo ningún `loan` **y** tampoco tiene ningún `source_movement` no manual (una segunda validación agregada más allá de lo pedido explícitamente, para cubrir el caso de un bolsillo elegido como destino de un pago sin haber originado nunca un préstamo — evita perder esa trazabilidad al borrar). `getFundingSources(includeArchived = false)` filtra archivados por defecto. `LoanRepository.getLoansBySource(sourceId)` nuevo, usado por las validaciones de archivado.
  - `FundingSourcesScreen`: enlace "Ver archivados"/"Ver activos", insignia "Archivado" en las filas. `FundingSourceDetailScreen`: botón de acción — "Eliminar" (con confirmación) si el bolsillo nunca tuvo préstamos, si no "Archivar"/"Reactivar" (deshabilitado con mensaje explicativo mientras haya préstamos activos/parciales); el FAB de "Agregar dinero" se oculta si el bolsillo está archivado.
  - Los selectores de bolsillo en `NewLoanScreen` y `RegisterPaymentSheet` ya llamaban a `getFundingSources()` sin argumentos, así que quedaron excluyendo archivados automáticamente con el nuevo default. Único ajuste necesario: `LoanDetailViewModel` usaba esa misma función para resolver nombres de bolsillo destino en el historial de pagos — se cambió a `includeArchived = true` ahí, porque un pago viejo puede apuntar a un bolsillo que ya se archivó y su nombre debe seguir viéndose.
  - Probado en dispositivo físico: (1) crear un bolsillo sin usar y borrarlo de verdad — funciona; (2) crear uno, usarlo en un préstamo, confirmar que "Archivar" queda deshabilitado con el mensaje explicativo mientras el préstamo sigue activo/atrasado; (3) confirmar que un bolsillo archivado no aparece en los selectores de operaciones nuevas y que su historial se sigue viendo bien desde su detalle. **Pendiente de una verificación rápida:** no se reconfirmó en el dispositivo el caso "archivar una vez el préstamo queda pagado" de punta a punta (una interrupción de sesión cortó esa prueba después de registrar el pago) — la ruta de código es simétrica a la del caso bloqueado ya confirmado y reutiliza la misma consulta de `canArchive`, pero vale la pena un chequeo rápido en la próxima sesión.
- Fase completada antes de esa: **Fase 5.5, paso 3 — extracción de strings a `strings.xml`** (tarea puramente mecánica: mover texto, sin tocar lógica de negocio, de datos, ni el diseño visual del paso 2).
  - `res/values/strings.xml` pasó de solo `app_name` a **119 `<string>` + 3 `<plurals>`** (122 recursos en total), agrupado con comentarios de sección por feature: Common, Auth, Home, Friends, Sources (bolsillos), Loans, Payments. Nombres en inglés/snake_case con prefijo por pantalla (`login_*`, `home_*`, `friend_*`/`friends_*`, `source_*`/`sources_*`, `movement_*`, `loan_*`/`loans_*`, `payment_*`), más un grupo `common_*` para texto idéntico reutilizado en varias pantallas (`common_cancel`, `common_save`, `common_delete`, `common_retry`, `common_name_label`, `common_amount_label`, `common_name_required_error`, `common_amount_invalid_error`, `common_unknown_friend`, `common_empty_value` para el placeholder "—", `common_source_with_balance` para "%1$s (%2$s)"). Algunos strings de error también se reutilizan entre pantallas cuando el texto es idéntico (ej. `sources_load_error`, `friends_load_error`, `friend_create_error`, `loan_due_date_field_label`, `loan_source_label`, `payment_register_action`).
  - Los `sealed interface *UiState.Error` y los `MutableStateFlow<String?>` de error sueltos en los ViewModels (`_saveError`, `_deleteError`, `_createError`, `_movementError`, etc.) cambiaron de cargar el `String` en español a cargar un **`@StringRes Int`** (el id del recurso) — los ViewModels no tienen `Context`/composición para resolver `stringResource()` directamente, así que el texto final se resuelve con `stringResource(...)` en el composable que lo muestra. Mismo patrón para `LoanVisualStatus.label: String` → `labelRes: Int` (usado por `StatusBadge`).
  - `LoanListItem.friendName` pasó de `String` (con fallback `?: "Amigo"` hardcodeado dentro del ViewModel) a `String?`, resolviendo el fallback `common_unknown_friend` con `stringResource()` en el composable — necesario porque el ViewModel no puede llamar a `stringResource()`.
  - Los contadores de `HomeScreen` (bolsillos/amigos/préstamos activos) pasaron del helper manual `pluralize()` a `<plurals>` reales de Android (`pluralStringResource`), el mecanismo idiomático para esto.
  - Compila limpio (`BUILD SUCCESSFUL`) y se verificó en dispositivo físico (modo oscuro) que todo el texto se ve igual que antes de la extracción: Home, Bolsillos (estado vacío + hoja "Nuevo bolsillo"), Amigos (estado vacío + diálogo "Nuevo amigo" + validación + banner de error con "Reintentar"), Préstamos (estado vacío), Nuevo préstamo (todas las labels + "Mostrar más" + el placeholder dinámico `Crear amigo "%1$s"` del `FriendPicker`).
  - **Nota para la próxima sesión:** durante esta QA, las consultas a Supabase para amigos y préstamos empezaron a fallar de forma intermitente (`FriendsUiState.Error` → "No se pudieron cargar los amigos.") mientras que bolsillos devolvía una lista vacía, pese a que la conectividad de red del dispositivo estaba bien (se confirmó con ping). Los datos de prueba que quedaban de fases anteriores (bolsillo "Presupuesto para prestamos", amigos Camilo/Daniel, préstamos de prueba) ya no aparecían. No se tocó ninguna capa de datos/repositorio en este paso, así que esto no lo causó la extracción de strings — probablemente el proyecto de Supabase (posiblemente free-tier) esté pausado o haya un problema de sesión/RLS. Revisar el estado del proyecto de Supabase antes de la próxima fase. Se alcanzó a crear un amigo de prueba ("TestQA") justo antes de que el fallo apareciera.
- Fase completada antes de esa: **Fase 5.5, paso 2 — diseño visual y consistencia** (solo diseño/UI, sin tocar lógica de negocio ni de datos, sin extraer strings todavía — eso fue el paso 3).
  - Tema M3 reescrito en `ui/theme/`: `Color.kt` define los roles completos de `ColorScheme` (light y dark) a mano, derivados del color semilla **`#0061A4`** (azul, transmite confianza/finanzas), más roles custom `success`/`warning` (con sus `on*`/`*Container`). `Theme.kt` ensambla `LightColorScheme`/`DarkColorScheme` y expone `StatusColors` (success/warning + variantes) vía `CompositionLocalProvider` + `MaterialTheme.statusColors`, como fuente única de verdad para los badges de estado de préstamo (antes había valores de color repetidos por pantalla). Se quitó `dynamicColor` (que antes defaulteaba a `true` en Android 12+) para que la paleta de marca elegida siempre gane sobre el wallpaper del usuario. No se agregó ninguna librería de generación de paletas (Material Color Utilities/HCT): no hay un artefacto oficial limpio para eso fuera del generador dinámico de Android 12+, y va en línea con la filosofía de dependencias mínimas del proyecto.
  - `Spacing.kt` (nuevo): `object Spacing { xs=4dp, sm=8dp, md=16dp, lg=24dp }`, aplicado en reemplazo de valores `dp` sueltos en prácticamente todas las pantallas (`LoginScreen`, `FriendsScreen`, `FriendPicker`, `LoansScreen`, `LoanDetailScreen`, `NewLoanScreen`, `FundingSourcesScreen`, `FundingSourceDetailScreen`, `RegisterPaymentSheet`, `HomeScreen`).
  - `HomeScreen` rediseñada como dashboard: `LazyVerticalGrid(GridCells.Fixed(2))` con una card por sección (Bolsillos/Amigos/Préstamos, mismo tamaño, ícono representativo + subtítulo con conteo rápido vía `HomeViewModel.loadCounts()` — cada conteo se carga independiente y falla en silencio a `null`, es puramente cosmético). Reemplazó los botones sueltos que había antes; de paso se eliminó `FriendPickerTestScreen.kt` (ya redundante, quedaba pendiente de una fase anterior) junto con su ruta y botón en `MainActivity.kt`/`HomeScreen.kt`.
  - Se agregó la dependencia **Material Icons Extended** (`androidx.compose.material:material-icons-extended`, gestionada por el BOM de Compose ya existente, sin versión explícita) porque `material-icons-core` solo traía ~48 íconos y no alcanzaba para billetera/personas/pagos del dashboard.
  - Consistencia visual entre pantallas: en listas nombre+monto, nombre a la izquierda y monto a la derecha en todas partes (estilo tabla contable) — incluye `LoansScreen`/`LoanDetailScreen` (con `StatusBadge` compartido, extraído a `ui/loans/StatusBadge.kt`, usado tanto en la lista como en el detalle) y `FundingSourcesScreen`/`FundingSourceDetailScreen`. Filas de botones múltiples (Cancelar/Guardar, Cancelar/Eliminar) unificadas con un `DialogButtonRow` reutilizable (`ui/common/DialogButtonRow.kt`, ancho igual vía `weight(1f)`), usado en los diálogos de `FriendsScreen` y `FundingSourceDetailScreen`. El formateo de pesos colombianos ya usaba `formatCop`/`CurrencyFormat.kt` en todas las pantallas — se confirmó que no había implementaciones duplicadas.
  - Bug encontrado y corregido durante la revisión visual en dispositivo físico: `DetailRow` en `LoanDetailScreen.kt` (usado para "Bolsillo de origen", "Fecha tentativa de pago", etc.) usaba `Arrangement.SpaceBetween` sin `weight()`, así que cuando el valor envolvía a dos líneas (ej. "Presupuesto para préstamos") quedaba pegado a la etiqueta sin espacio y sin alinear a la derecha. Se corrigió dando a ambos `Text` `Modifier.weight(1f)` y `textAlign = TextAlign.End` al valor.
  - Revisado visualmente en dispositivo físico, en modo claro y modo oscuro, cada pantalla (Home, bolsillos, detalle de bolsillo, amigos + diálogos editar/eliminar, préstamos, nuevo préstamo, detalle de préstamo, registrar pago, login): contraste, alineación y tamaños correctos en ambos modos.
- Fase completada antes de esa: **Fase 5.5, paso 1 — editar/borrar amigos y movimientos manuales de bolsillo** (funcionalidad que había quedado faltando de fases anteriores; sin tocar diseño visual ni extraer strings).
  - `FriendRepository.updateFriend(id, name, phone, notes)` y `deleteFriend(id)`. `deleteFriend` captura `PostgrestRestException` con `code == "23503"` (violación de foreign key de Postgres — `loans.friend_id` no tiene `ON DELETE`) y la envuelve en `FriendHasLoansException` propia, mismo patrón que `LoanSourceMovementException` de la Fase 4.
  - `FriendsViewModel`/`FriendsScreen`: el diálogo de creación se generalizó a un único `FriendDialog` reutilizado para crear y editar (precargado vía `remember(editingFriend)` cuando hay un amigo en edición); `FriendRow` con íconos de lápiz/basura; confirmación (`AlertDialog`) antes de borrar, mostrando el mensaje de `FriendHasLoansException` si aplica sin cerrarse.
  - `FundingSourceRepository.updateMovement(movementId, movementType, amount, notes)` y `deleteMovement(movementId)`: ambos validan primero que el movimiento sea manual (`reference_loan_id` y `reference_payment_id` ambos null; si no, lanzan `NonManualSourceMovementException`), revierten el efecto anterior sobre `current_balance` (resta si era income, suma si era outflow) y aplican el nuevo (o ninguno, si se borra).
  - `FundingSourceDetailViewModel`/`FundingSourceDetailScreen`: el bottom sheet de "Agregar dinero" se generalizó a `MovementSheet` (crear/editar, mismo patrón que `FriendDialog`); `MovementRow` solo muestra íconos de editar/borrar si el movimiento es manual — si viene de un préstamo o un pago, en su lugar muestra una etiqueta de solo lectura ("Préstamo"/"Pago") y ningún ícono de acción.
  - Se agregó `formatPlainAmount(Double)` en `CurrencyFormat.kt` (número plano sin símbolo/agrupación, ej. "50000") para precargar el campo de monto editable — `formatCop` no sirve para eso porque agrega el símbolo `$` y separadores de miles.
  - Probado en dispositivo físico contra Supabase real: editar el nombre de un amigo se refleja en la lista; borrar un amigo con préstamos muestra el mensaje claro sin crash; borrar uno sin préstamos lo quita de la lista; editar el monto de un movimiento manual recalcula `current_balance` exactamente (revierte el viejo efecto + aplica el nuevo); borrar un movimiento manual también ajusta el saldo correctamente; los movimientos con `reference_loan_id`/`reference_payment_id` nunca muestran acciones de editar/borrar.
- Fase completada antes de esa: **Fase 5 — Payments (pagos)**. `LoanRepository.registerPrincipalPayment(loanId, principalPaid)`: recalcula `outstanding_principal = max(0, ...)` y deriva `status` (`paid` si llega a 0, `partial` si queda saldo pero es menor al `principal_amount` original, si no lo deja igual) — nunca a partir del interés. Se agregó `object LoanStatus` (constantes `ACTIVE`/`PARTIAL`/`PAID`/`OVERDUE`) para reemplazar los strings sueltos que había en `LoanRepository` y `LoanVisualStatus`. `PaymentRepository` (singleton) con `getPayments(loanId)` y `createPayment(loanId, principalPayment, interestPayment, principalDestinationSourceId, interestDestinationSourceId)`: inserta el `payment`, registra hasta dos `source_movements` de tipo `income` (uno de capital, otro de interés si es > 0 — al bolsillo de interés o, si es null, al mismo del capital) vía `FundingSourceRepository.recordSourceMovement`, y llama a `registerPrincipalPayment` para cerrar el ciclo. `ui/payments/RegisterPaymentViewModel`/`RegisterPaymentSheet`: `ModalBottomSheet` independiente (paquete propio, sin acoplarse a `LoanDetailViewModel`) abierto desde `LoanDetailScreen`, con saldo pendiente de referencia, campos de capital/interés (exige que al menos uno sea > 0), total en vivo, dropdown de bolsillo destino del capital preseleccionado con el bolsillo de origen del préstamo, y "Mostrar más" con el destino opcional del interés. `LoanDetailViewModel`/`LoanDetailScreen` actualizados: cargan los pagos junto con el préstamo (resolviendo nombres de bolsillo destino en memoria), muestran el botón "Registrar pago" solo si `status != "paid"`, y el historial real (capital, interés, bolsillo(s) destino) reemplazando el estado vacío. Ciclo completo de dos pagos parciales (capital + interés cada uno, a bolsillos separados) probado en dispositivo físico contra Supabase real: el préstamo pasó de `active` → `partial` → `paid`, el bolsillo acumuló los montos exactos en movimientos separados y trazados, y el botón "Registrar pago" desapareció al llegar a `paid`.
- **Anomalía de la Fase 5 investigada — cerrada, sin causa raíz encontrada, pero se blindó el flujo.** El intento fallido de crear un préstamo con interés fijo (3%) y vencimiento a 2 meses (bolsillo no descontado, lista de préstamos con error al recargar) **no se pudo reproducir** repitiendo exactamente el mismo caso (mismo `interest_type = "fixed"`, `interest_value`, fecha a 2 meses) con logcat completo. Se descartó explícitamente la hipótesis de una etiqueta en español (ej. "Fijo") colándose sin mapear a una columna que espera inglés: se revisó cada selector que escribe un valor fijo en Supabase (`interest_type`, `movement_type`, `status`) y los tres siempre pasan por `.dbValue` de un enum (`InterestType`, `MovementType`) o por `LoanStatus` — nunca el texto del botón. La explicación más probable es un tropiezo de la automatización de pruebas de esa sesión (taps mal ubicados) combinado posiblemente con un glitch transitorio de red/sesión. De todos modos, se blindó el flujo de dinero contra este tipo de falla independientemente de la causa:
  - `LoanRepository.createLoan` ya no falla en silencio si el `loan` se inserta pero `recordSourceMovement` (el descuento del bolsillo) falla: lanza `LoanSourceMovementException(loanId, cause)`.
  - `NewLoanViewModel` captura esa excepción específicamente y muestra "El préstamo se creó, pero no se pudo descontar el bolsillo de origen. Revísalo manualmente..." — y deliberadamente no navega de vuelta, para que el aviso no desaparezca de pantalla antes de leerse.
  - `LoanRepository.getLoans()` ahora decodifica cada fila del JSON por separado (`runCatching` por fila) en vez de decodificar la lista completa de una vez: un préstamo puntual con un valor no deserializable se omite en vez de tumbar toda la lista.
  - `LoansViewModel.loadLoans()`: el fetch de nombres de amigos es independiente — si `FriendRepository.getFriends()` falla, la lista de préstamos igual se muestra (con "Amigo" como placeholder) en vez de fallar por completo.
  - Sigue pendiente (no se implementó, no bloquea): una transacción atómica real de Postgres para `loan` + `source_movement`; por ahora el error simplemente queda visible y explicado si la segunda escritura falla.
- Cómo se traen amigo/bolsillo junto a cada préstamo o pago: **join en memoria**, no embedding de foreign keys de supabase-kt. `LoansViewModel` pide `LoanRepository.getLoans()` + `FriendRepository.getFriends()` y arma un mapa `friendId -> name`; `LoanDetailViewModel` pide el `Loan`, busca el `Friend` en `FriendRepository.getFriends()`, el `FundingSource` de origen con `FundingSourceRepository.getFundingSource(id)`, y para el historial de pagos arma un mapa `sourceId -> name` con `FundingSourceRepository.getFundingSources()`. Se eligió así por ser lo más simple de implementar bien; con el volumen de datos de un solo usuario el costo de las consultas extra es irrelevante.
- Nota técnica: el BOM de Compose del proyecto resuelve a **Material3 1.4.0**, donde `ExposedDropdownMenu` y `Modifier.menuAnchor(...)` ya no son funciones top-level sino miembros de `ExposedDropdownMenuBoxScope` (se llaman sin import, dentro del lambda de `ExposedDropdownMenuBox`), y `MenuAnchorType` quedó deprecado a favor de `ExposedDropdownMenuAnchorType`. Tenerlo en cuenta para cualquier otro selector tipo dropdown que se agregue más adelante.
- Nota técnica: los `ModalBottomSheet` con varios campos necesitan `.verticalScroll(rememberScrollState()).imePadding()` en su `Column` interno — sin eso, con el teclado abierto los campos de más abajo (Nota, botón Guardar) quedaban físicamente detrás del teclado e inalcanzables. Ya corregido en `FundingSourcesScreen`, `FundingSourceDetailScreen` y `RegisterPaymentSheet`; replicar en bottom sheets futuros.
- Queda en la base de datos de Supabase un bolsillo de prueba ("Presupuesto para prestamos", con movimientos "Saldo inicial" y "Dinero 50000" — el movimiento "Retiro de prueba" que se había creado en la Fase 2 se editó y luego se borró durante las pruebas de edición/borrado de la Fase 5.5) creado durante las pruebas de la Fase 2; dos amigos de prueba ("Camilo", "Daniel") creados durante las pruebas de la Fase 3 (un tercer amigo, "Pedroniel", se editó a "Pedro" y luego se borró durante las pruebas de la Fase 5.5, ya que no tenía préstamos asociados); un préstamo de prueba (Daniel, $1.000.000, bolsillo "Presupuesto para prestamos", vencimiento 14 oct. 2026, interés fijo informativo guardado como "109" por un error de tecleo durante la prueba, sin relevancia funcional) creado durante las pruebas de la Fase 4; un préstamo ya pagado (Camilo, $10.000.000, dos pagos de 5.000.000 capital + 300.000 interés cada uno) creado durante las pruebas de la Fase 5; y un préstamo adicional (Daniel, $100.000, interés fijo 3%, vencimiento 14 nov. 2026) creado al reproducir la anomalía de la Fase 5 — el bolsillo sí se descontó correctamente en este caso. El usuario decidió no borrar estos datos de prueba restantes por ahora.
