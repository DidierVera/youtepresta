package com.didiprogrammer.youtepresta.ui.theme

import androidx.compose.ui.unit.dp

/**
 * The app's only spacing scale. Use these instead of one-off `.dp` values so paddings and gaps
 * stay consistent across screens — see CLAUDE.md's Fase 5.5 design pass.
 */
object Spacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 16.dp
    val lg = 24.dp
}
